# How to build and run YInit

Install requirments: 
```
# 'cython3' will pull also python3: 
apt install cython3 
```

To build project run: 
```bash
make
```

Compiled binary is in `./build/yinit-bin`.


```bash
python -m build .
```

This will place compiled packages in `./dist` directory.

