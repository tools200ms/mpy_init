[Service]
description = BusyBox NTP daemon

requires = network

# Service defined targets:
provides = time
defines = online

# process control
exec_start = /bin/busybox ntpd
pid_file = /var/run/ntpd.pid
