
description = BusyBox SyslogD

requires = clock,hostname,localmount

# Service defined targets:
provides = logger

# process control
exec_start = /bin/busybox syslogd
pid_file = /var/run/syslogd.pid
