[Service]
description = Provides 'software' clock (set the time based on last shutdown timastamp)

# Service order and dependency control:
after = modules
before = fsck
provides = clock

# process control
mpy_package = mpy_launch_sclock

